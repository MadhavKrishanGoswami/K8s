apiVersion: v1
kind: Service
metadata:
  name: postgress
  labels:
    name: postgress-service
    app: demo-voting-app
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    name: postgress-pod
    app: demo-voting-app
